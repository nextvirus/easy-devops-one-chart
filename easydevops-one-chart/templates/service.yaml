apiVersion: v1
kind: Service
metadata:
  name: {{ include "easydevops-one-chart.fullname" . }}
  labels:
    {{- include "easydevops-one-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "easydevops-one-chart.selectorLabels" . | nindent 4 }}
